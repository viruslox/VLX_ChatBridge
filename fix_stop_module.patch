--- internal/core/module/manager.go
+++ internal/core/module/manager.go
@@ -123,6 +123,11 @@
		return fmt.Errorf("module %s not found", name)
	}

+	st := m.states[name]
+	if st == nil || !st.running {
+		return nil
+	}
+
	log.Printf("Stopping module: %s...", name)
	if err := mod.Stop(); err != nil {
		m.setState(name, false, err)
